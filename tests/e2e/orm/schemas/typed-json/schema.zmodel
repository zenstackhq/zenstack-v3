datasource db {
    provider = "sqlite"
    url      = "file:./dev.db"
}

model User {
    id      Int     @id @default(autoincrement())
    profile Profile @json
}

enum Gender {
    MALE
    FEMALE
}

type Profile {
    age     Int
    name    String
    gender  Gender?
    jobs    Job[]
    address Address?
}

type Address {
    country String
}

type Job {
    title String
}
